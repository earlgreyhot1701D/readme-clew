# flexible-cli

## Installation
If you have npm: `npm install -g flexible-cli`
If you have yarn: `yarn global add flexible-cli`

## Configuration
Set `FLEXIBLE_CONFIG_PATH` if you want a custom config location.
You can use either YAML or JSON. The tool detects which.
