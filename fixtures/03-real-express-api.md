# weather-api

A weather proxy using OpenWeather.

## Environment
- `OPENWEATHER_KEY` — required
- `PORT` — defaults to 3000

## Running
```bash
npm install
npm start
```

## Dependencies
- express
- node-fetch
- cors

Tests use jest. Run `npm test`.

![architecture](./docs/arch.png)
